spring:
  datasource:
    url: jdbc:mysql://localhost:3306/zee5assessment
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    properties:
      hibernate:
        dialect:  org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    hibernate:
      ddl-auto: update
server:
  port: 61742
  servlet:
    context-path: /api

zee5assessment:
  app:
    jwtSecret: secretkey
    jwtExpirationMs: 86400000

